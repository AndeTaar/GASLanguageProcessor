canvas (250*10, 10*50, Colour(255, 255, 255, 1));

number x = 0;

circle CircleCreator(point Center, number Width, number Stroke, colour Fill, colour StrokeColour)
{
    return Circle(Center, Width, Stroke, Fill, StrokeColour);
}

colour green = Colour(0, 255, 0, 1);
colour black = Colour(255, 100, 100, 1);

list<circle> circles = list<circle>();
number i;

for(number i = 0; i < 10; i = i + 1;)
{
   circles.add(CircleCreator(Point(50, 10), 40+i, 10, green, black))
}

colour white = Colour(255, 255, 255, 1);
square firkant = Square(Point(10,10), 180, 10, green, black);
text txt = Text("Its even worse for mouses!", Point(0,0), "Arial", 14, black);


for(number i = 0; x < 10; x = x + 1;)
{
    square firkant2 = Square(Point(10, 10 + x * 50), 180, 10, green, black);
    text txt2 = Text("Its even worse for mouses!", Point(0,0 + x * 50), "Arial", 14, black);
}

bool GetBool()
{
    return true;
}

group mouse = Group(Point(100, 50),
{
    group mousEars = Group(Point(0, 0),
    {
        circle leftEar = Circle(Point(30, 0), 37, 10, black, black);,
        circle rightEar = Circle(Point(30, 0), 37, 10, black, black);
    });,

    group mouseFace = Group(Point(0, 0),
    {
        circle leftEye = Circle(Point(-30, 0), 30, 10, white, black);,
        circle rightEye = Circle(Point(30, 0), 30, 10, white, black);,
        circle nose = Circle(Point(0, 30), 50, 10, white, black);
    });
});

colour reds = Colour(255, 0, 0, 1);
point xy = Point(0, 0);
line aline = Line(xy, Point(200 * 10, 200), 10, reds);
line line1 = Line(Point(200, 0), Point(0, 200), 10, reds);
rectangle rectangle1 = Rectangle(Point(10, 150), Point(150, 300), 10, black, reds);

number y = 9;
if (x > 4 || 2 < 9) {
  x = 8;
} else if (GetBool()) {
  x = 9;
  y = 8;
} else {
  x = 5;
  y = 4;
}
