
number x = 0;
colour green = Colour(0, 255, 0, 1);
colour black = Colour(0, 0, 0, 1);
colour white = Colour(255, 255, 255, 1);
square firkant = Square(Point(10,10), 180, green, black);
text txt = Text(Point(0,0), "Its even worse for mouses!", 14, "Arial", black);


for(number i = 0; x < 10; x = x + 1;)
{
    square firkant = Square(Point(10,10 + x * 50), 180, green, black);
    text txt = Text(Point(0,0 + x * 50), "Its even worse for mouses!", 14, "Arial", black);
}

circle CircleCreator(point Center, number Width, number Stroke, colour Fill, colour StrokeColour)
{
    return Circle(Center, Width, Stroke, Fill, StrokeColour);
}

bool GetBool()
{
    return true;
}

circle cirkel = CircleCreator(Point(250, 250), 100, 10, green, black);

group mouse = Group(Point(100, 50),
{
    group mousEars = Group(Point(0, 0),
    {
        circle leftEar = Circle(Point(30, 0), 37, 10, black, black);,
        circle rightEar = Circle(Point(30, 0), 37, 10, black, black);
    });,

    group mouseFace = Group(Point(0, 0),
    {
        circle leftEye = Circle(Point(-30, 0), 30, 10, white, black);,
        circle rightEye = Circle(Point(30, 0), 30, 10, white, black);,
        circle nose = Circle(Point(0, 30), 50, 10, white, black);
    });
});

colour red = Colour(255, 0, 0, 1);
point xy = Point(0, 0);
line aline = Line(xy, Point(200 * 10, 200), 10, red, black);
line line1 = Line(Point(200, 0), Point(0, 200), 10, red, black);
rectangle rectangle1 = Rectangle(Point(10, 150), 180, 10, black, red);

number y = 9;
if (x > 4 || 2 < 9) {
  x = 8;
} else if (GetBool()) {
  x = 9;
  y = 8;
} else {
  x = 5;
  y = 4;
}
